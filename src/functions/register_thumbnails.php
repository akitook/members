<?php

/**
 * サムネイル（アイキャッチ）画像の設定
 *
 **/

/*
function default_thumbnail(){
 // サムネイルサイズ設定
  if ( function_exists( 'add_image_size' ) ) {
    add_image_size( 'item_thumbnail',820, 472, true );
  }
}

add_action( 'save_post', 'default_thumbnail' );
*/
