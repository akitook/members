const setupYubinbango = () => {
  const form = document.querySelector(".mw_wp_form_input form");
  if (form) {
    document.querySelector(".mw_wp_form_input form").classList.add("h-adr");
  }
};

export default setupYubinbango();
